function StartTheCountdown(){
    //This function initiates the launch sequence by way of the "StartTheCountdown" function being called via the onclick event trigger linked to the button on line 16 of the.
//Declaration of the variables occurs below.
var countdown = 10;
//Used to keep track of real time.
//Time unit used is milliseconds.
var timeout = 10000
setTimeout(() => {
    document.getElementById("CountdownDisplay").innerHTML = "BLASTOFF";
    countdown = countdown - 1;
}, timeout)
timeout = timeout - 1000;
//Time until launch is decremented by one each time this script is called
setTimeout(() => {
    document.getElementById("CountdownDisplay").innerHTML = countdown;
    countdown = countdown - 1;
}, timeout)
timeout = timeout - 1000;
//Time until launch is decremented by one each time this script is called
setTimeout(() => {
    document.getElementById("CountdownDisplay").innerHTML = countdown;
    countdown = countdown - 1;
}, timeout)
timeout = timeout - 1000;
//Time until launch is decremented by one each time this script is called
setTimeout(() => {
    document.getElementById("CountdownDisplay").innerHTML = countdown;
    countdown = countdown - 1;
}, timeout)
timeout = timeout - 1000;
//Time until launch is decremented by one each time this script is called
setTimeout(() => {
    document.getElementById("CountdownDisplay").innerHTML = countdown;
    countdown = countdown - 1;
}, timeout)
timeout = timeout - 1000;
//Time until launch is decremented by one each time this script is called
setTimeout(() => {
    document.getElementById("CountdownDisplay").innerHTML = countdown;
    countdown = countdown - 1;
}, timeout)
timeout = timeout - 1000;
//Time until launch is decremented by one each time this script is called
setTimeout(() => {
    document.getElementById("CountdownDisplay").innerHTML = countdown;
    countdown = countdown - 1;
}, timeout)
timeout = timeout - 1000;
//Time until launch is decremented by one each time this script is called
setTimeout(() => {
    document.getElementById("CountdownDisplay").innerHTML = countdown;
    countdown = countdown - 1;
}, timeout)
timeout = timeout - 1000;
//Time until launch is decremented by one each time this script is called
setTimeout(() => {
    document.getElementById("CountdownDisplay").innerHTML = countdown;
    countdown = countdown - 1;
}, timeout)
timeout = timeout - 1000;
//Time until launch is decremented by one each time this script is called
setTimeout(() => {
    document.getElementById("CountdownDisplay").innerHTML = countdown;
    countdown = countdown - 1;
}, timeout)
timeout = timeout - 1000;
//Time until launch is decremented by one each time this script is called
setTimeout(() => {
    document.getElementById("CountdownDisplay").innerHTML = countdown;
    countdown = countdown - 1;
}, timeout)
timeout = timeout - 1000;}
//Time until launch is decremented by one each time this script is called